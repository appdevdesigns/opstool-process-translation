<div class="form-inline">
    <div class="col-xs-6">
        <label for="translateFromLanguage">From :</label>
        <select id="translateFromLanguage" class="form-control input-sm language-selector" trrequest-trans="fromLanguage">
            <% for (var code in fromLanguagesList.attr()) { %>
                <option value="<%= code %>">
                    <%= fromLanguagesList[code] %>
                </option>
                <% } %>
        </select>
    </div>
    <div class="col-xs-6">
        <label for="translateToLanguage">To :</label>
        <select id="translateToLanguage" class="form-control input-sm language-selector" trrequest-trans="toLanguage">
            <% for (var code in toLanguagesList.attr()) { %>
                <option value="<%= code %>">
                    <%= toLanguagesList[code] %>
                </option>
                <% } %>
        </select>
    </div>
</div>